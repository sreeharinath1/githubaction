name: Pull Request Events Demo

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Show event type
        run: echo "Triggered by ${{ github.event.action }}"
